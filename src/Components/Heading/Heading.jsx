import React from 'react';

const Heading = ({title}) => {
    return (
        <div className='py-12'>
            <h1 className='text-3xl font-bold'>{title}</h1>
        
        </div>
    );
};

export default Heading;